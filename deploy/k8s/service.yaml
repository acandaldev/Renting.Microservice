apiVersion: v1
kind: Service
metadata:
  name: renting-microservice
spec:
  selector:
    app: renting-microservice
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
